language: python

python:
    - "2.6"
    - "2.7"

# Install FFmpeg.
before_script: sudo apt-get install -qq vlc libvlc-dev

# Install build requirements.
install: pip install --use-mirrors cython

# Currently only building, not testing.
script: make build
